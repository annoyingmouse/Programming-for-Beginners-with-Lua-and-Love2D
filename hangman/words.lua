local word = require("Word")
local margin = 50
local width, height = love.graphics.getDimensions()

WordsToGuess = {
  word:new(margin, "apple", width, height),
  word:new(margin, "horizon", width, height),
  word:new(margin, "computer", width, height),
  word:new(margin, "music", width, height),
  word:new(margin, "love", width, height),
  word:new(margin, "space", width, height),
  word:new(margin, "technology", width, height),
  word:new(margin, "internet", width, height),
  word:new(margin, "development", width, height),
  word:new(margin, "artificial", width, height),
  word:new(margin, "bread", width, height),
  word:new(margin, "chair", width, height),
  word:new(margin, "apple", width, height),
  word:new(margin, "table", width, height),
  word:new(margin, "window", width, height),
  word:new(margin, "garden", width, height),
  word:new(margin, "carpet", width, height),
  word:new(margin, "bottle", width, height),
  word:new(margin, "kettle", width, height),
  word:new(margin, "biscuit", width, height),
  word:new(margin, "jumper", width, height),
  word:new(margin, "trousers", width, height),
  word:new(margin, "pavement", width, height),
  word:new(margin, "colour", width, height),
  word:new(margin, "centre", width, height),
  word:new(margin, "theatre", width, height),
  word:new(margin, "organise", width, height),
  word:new(margin, "neighbour", width, height),
  word:new(margin, "holiday", width, height),
  word:new(margin, "railway", width, height),
  word:new(margin, "village", width, height),
  word:new(margin, "country", width, height),
  word:new(margin, "market", width, height),
  word:new(margin, "letter", width, height),
  word:new(margin, "pencil", width, height),
  word:new(margin, "notebook", width, height),
  word:new(margin, "library", width, height),
  word:new(margin, "museum", width, height),
  word:new(margin, "station", width, height),
  word:new(margin, "airport", width, height),
  word:new(margin, "seaside", width, height),
  word:new(margin, "hillside", width, height),
  word:new(margin, "woodland", width, height),
  word:new(margin, "morning", width, height),
  word:new(margin, "evening", width, height),
  word:new(margin, "weekend", width, height),
  word:new(margin, "journey", width, height),
  word:new(margin, "traffic", width, height),
  word:new(margin, "weather", width, height),
  word:new(margin, "thunder", width, height),
  word:new(margin, "lightning", width, height),
  word:new(margin, "rainbow", width, height),
  word:new(margin, "harvest", width, height),
  word:new(margin, "farmer", width, height),
  word:new(margin, "baker", width, height),
  word:new(margin, "butcher", width, height),
  word:new(margin, "candle", width, height),
  word:new(margin, "lantern", width, height),
  word:new(margin, "blanket", width, height),
  word:new(margin, "pillow", width, height)
}
